version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: log_platform_postgres
    environment:
      POSTGRES_DB: db_zj_60077
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: dev123456
    ports:
      - "15077:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dev -d db_zj_60077"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: log_platform_redis
    ports:
      - "16377:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:
